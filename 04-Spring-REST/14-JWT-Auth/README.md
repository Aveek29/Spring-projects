# JWT Authentication

Spring Boot application with Spring Security and JWT-based stateless authentication. Includes user registration, login, JWT token generation/validation, and secured REST endpoints.
