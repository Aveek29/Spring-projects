# demoExamplewebSpringBott

Calculator web application with JSP views and form handling.

**Tech:** Spring Boot 3, JSP, Maven

**Fixes applied:** `Homecontroller` → `HomeController` rename, tomcat-jasper version fix, snapshot repositories removed.
