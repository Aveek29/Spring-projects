# Using ModelAttribute

Demonstrates `@ModelAttribute` to automatically bind form fields to a model object and add common data to all controller methods. Also shows `@ModelAttribute` method-level usage.
