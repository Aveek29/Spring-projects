# Setting Prefix and Suffix

Configures Spring MVC's view resolver prefix and suffix so controller methods return logical view names instead of full JSP paths. Simplifies view resolution configuration.
