# Sending data to Controller (Thymeleaf path)

Spring Boot app demonstrating form data submission via URL parameters mapped to a controller method using `HttpServletRequest.getParameter()`. Renders Thymeleaf views.
