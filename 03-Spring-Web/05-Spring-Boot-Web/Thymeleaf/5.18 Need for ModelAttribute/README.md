# Need for ModelAttribute (Thymeleaf path)

Shows manual object construction from individual `@RequestParam` values. Sets the stage for why `@ModelAttribute` is useful by demonstrating the verbose alternative.
