# Servlet Mapping (Thymeleaf path)

Configures servlet mapping in embedded Tomcat by adding a servlet context and mapping a URL pattern (`/hello`) to a custom `HelloServlet`. Demonstrates basic servlet registration and routing.
