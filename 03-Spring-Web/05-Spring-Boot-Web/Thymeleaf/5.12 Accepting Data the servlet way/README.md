# Accepting Data the Servlet Way (Thymeleaf path)

Accepts user input via `HttpServletRequest` parameters, performs addition, and forwards to a Thymeleaf result view. Demonstrates servlet-style data handling in Spring MVC.
