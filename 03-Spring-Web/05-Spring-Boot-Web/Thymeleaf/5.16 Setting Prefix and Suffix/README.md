# Setting Prefix and Suffix (Thymeleaf path)

Configures Spring MVC's view resolver prefix and suffix so controller methods return logical view names instead of full template paths. Simplifies view resolution configuration.
