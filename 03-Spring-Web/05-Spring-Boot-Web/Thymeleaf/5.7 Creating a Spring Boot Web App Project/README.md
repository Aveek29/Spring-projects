# Spring Boot Web App (Thymeleaf path)

Minimal Spring Boot web application with `@SpringBootApplication` and the main entry point. Serves as the foundation for adding web controllers and Thymeleaf views.
