# Model Object (Thymeleaf path)

Introduces Spring MVC's `Model` interface to pass data from controller to Thymeleaf view. Attributes added to the model are accessible in templates without session coupling.
