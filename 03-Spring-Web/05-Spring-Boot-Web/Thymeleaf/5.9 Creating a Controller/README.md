# Creating a Controller (Thymeleaf path)

Introduces `@Controller` in Spring MVC with a `home()` method returning a view name. Demonstrates the basic request-to-view mapping pattern for Thymeleaf templates.
