# 3.9 Scope And Value Annotation

Demonstrates `@Value` for injecting literal values and `@Scope` for bean scope control using annotations. Combines `@Primary`, `@Qualifier`, and `@Component` for full annotation-driven config.
