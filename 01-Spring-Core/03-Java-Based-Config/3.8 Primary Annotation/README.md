# 3.8 Primary Annotation

Shows the `@Primary` annotation on a bean to mark it as the preferred candidate when multiple beans implement the same interface. Combined with `@Qualifier` for explicit selection.
