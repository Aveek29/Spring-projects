# 03-Java-Based-Config

9 lessons on Java-based configuration: @Bean, @Configuration, @ComponentScan, @Scope, @Autowired, @Primary, @Qualifier, @Value.

**Tech:** Spring 6, Maven, JUnit 5

**Fixes applied:** JUnit 5 upgrade, .classpath JRE fixes.
