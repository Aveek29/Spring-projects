# 3.6 Component Stereotype Annotation

Introduces `@Component` and `@ComponentScan` annotations for automatic bean discovery. No explicit `@Bean` methods needed—Spring scans and registers components automatically.
