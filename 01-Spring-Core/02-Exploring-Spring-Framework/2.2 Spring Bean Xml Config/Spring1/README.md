# 2.2 Spring Bean XML Config

Demonstrates loading beans from an XML configuration file (`spring.xml`) using `ClassPathXmlApplicationContext`. Alien object is created and managed by the Spring container.
