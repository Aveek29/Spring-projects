# 04-Spring-JDBC

6 lessons on Spring JDBC: JdbcTemplate, RowMapper, schema/data SQL files, integration with H2 and PostgreSQL.

**Tech:** Spring Boot 3, Spring JDBC, H2, PostgreSQL, Maven

**Fixes applied:** `addStudent` compilation error fixed, H2 config added, unused ArrayList imports removed.
